import React, { ReactElement } from 'react'

interface Props {
  
}

export default function EditCurrencyAccount({}: Props): ReactElement {
  return (
    <div>
      EditCurrencyAccount
    </div>
  )
}
