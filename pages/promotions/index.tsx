import { MainWrapper } from "@/components/Wrappers"
import React, { ReactElement } from "react"

interface Props {}

export default function Promotions({}: Props): ReactElement {
  return <MainWrapper>Promotions</MainWrapper>
}
