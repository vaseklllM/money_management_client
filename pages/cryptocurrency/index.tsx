import { MainWrapper } from "@/components/Wrappers"
import React, { ReactElement } from "react"

interface Props {}

export default function Cryptocurrency({}: Props): ReactElement {
  return <MainWrapper>cryptocurrency</MainWrapper>
}
